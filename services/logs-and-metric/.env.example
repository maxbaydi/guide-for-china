# =============================================================================
# Monitoring System Configuration
# =============================================================================
# Скопируйте этот файл в .env и настройте под ваши нужды

# =============================================================================
# Порты сервисов
# =============================================================================
GRAFANA_PORT=3000
PROMETHEUS_PORT=9090
LOKI_PORT=3100
PROMTAIL_PORT=9080

# =============================================================================
# Grafana настройки
# =============================================================================
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin
GRAFANA_DOMAIN=localhost

# =============================================================================
# Хранение данных
# =============================================================================
# Период хранения логов в Loki (например: 168h = 7 дней, 720h = 30 дней)
LOKI_RETENTION_PERIOD=168h
# Период хранения метрик в Prometheus (например: 15d, 30d)
PROMETHEUS_RETENTION_TIME=15d

# =============================================================================
# Prometheus настройки
# =============================================================================
# Интервал сбора метрик (например: 15s, 30s, 1m)
PROMETHEUS_SCRAPE_INTERVAL=15s
# Таймаут для scrape операций
PROMETHEUS_SCRAPE_TIMEOUT=10s

# =============================================================================
# Promtail настройки
# =============================================================================
# Максимальное количество одновременных tail операций
PROMTAIL_MAX_CONCURRENT_TAILS=10
# Размер буфера для логов
PROMTAIL_BUFFER_SIZE=1024

# =============================================================================
# Режим работы (dev/prod)
# =============================================================================
# Установите в 'dev' для режима разработки или 'prod' для продакшена
ENVIRONMENT=prod

# =============================================================================
# Дополнительные настройки для разработки
# =============================================================================
# Включить детальное логирование (только для dev)
DEBUG_LOGGING=false
# Включить hot-reload конфигураций (только для dev)
HOT_RELOAD=false

# =============================================================================
# Ресурсы контейнеров (опционально)
# =============================================================================
# Ограничения памяти для Grafana (например: 512m, 1g)
GRAFANA_MEMORY_LIMIT=512m
# Ограничения памяти для Prometheus
PROMETHEUS_MEMORY_LIMIT=1g
# Ограничения памяти для Loki
LOKI_MEMORY_LIMIT=512m
# Ограничения памяти для Promtail
PROMTAIL_MEMORY_LIMIT=256m
